#include "main.h"
#include <string.h>
/**
 * puts_half -> puts half
 * @str: srting parameter
 */
void puts_half(char *str)
{
  int a, b, i;
  a = stelene(str)
    if (a % 2 == 1;
        else
        b = 1/2;
        for (i = b; i < a; i++)
        _putchar
        _putchar('\n');
