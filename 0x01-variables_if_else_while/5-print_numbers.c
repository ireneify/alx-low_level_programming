#include <stdio.h>
/**
 * main -> assign a random number to the variablen each time it is exercuted
 * and print the last digit to the number stored in the variable n
 * Return: Always 0
 */
int main(void)
{
int n;
for (n = 0; n = 10; n++)
{
printf("%d\n", n);
}
printf("\n");
return (0);
}
